<?xml version="1.0"?>
<odoo>

  <!-- This record holds the action for complaint report -->
  <record id="complaint_report" model="ir.actions.report">
        <field name="name">Complaint Report</field>
        <field name="model">member.complaint</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">members_custom.complaint_reports</field>
        <field name="report_file">members_custom.complaint_reports</field>
        <field name="binding_model_id" ref="model_member_complaint"/>
        <field name="binding_type">report</field>
   </record>

</odoo>
